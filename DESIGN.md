# DESIGN.md · 原研哉式 Web UI 设计规范

**版本**：v1.0
**制定日期**：2026-09-11
**文档性质**：可独立分发的设计规范。自包含，无外部文件依赖。
**适用范围**：Web 端产品界面 —— 内容站、个人站、工具类产品、商业站点、App 的 Web 容器。
**不适用**：海报 / 封面 / 印刷物料 / 图文卡片等纯视觉层。那一层另有规范，本文件只约束**界面**。
**下次复核建议**：会员类 App 改版后的可用性实测、生成式界面（对话式 / Agent 式）对「空容器」模型的挑战、AI 检索对内容深度的要求变化。
**使用方式**：本文件同时给人和 AI 读。§1–§2 是判断依据，§3 是可直接复制的 token，§5–§9 是各层规则，§10 是评审门禁（不可跳过的验收口径）。

---

## 0. 一页速览

### 0.1 十条铁律

| # | 铁律 | 依据 |
|---|---|---|
| 1 | **白是关系，不是颜色。**底用暖近白，不用纯白；留白承担分组与节奏，不承担「高级感」 | 「白」是感知起点，不是色号 |
| 2 | **空是容器，不是缺失。**被删掉的东西必须仍有可见、可预期的取得路径 | Emptiness 与极简的分野 |
| 3 | **层级只用三样：字号、字重、留白。**不用颜色分层级 | 排版的「纯化」 |
| 4 | **一个屏幕只讲一件事。**主按钮只出现 1 个，强调色不超过 3 处 | 单一焦点原则 |
| 5 | **主导航项 ≤5 且永远可见。**不用汉堡菜单承载主导航 | 隐藏导航使可发现性降低近一半（NN/g 研究） |
| 6 | **有文字就用文字。**无标签图标不得作为唯一入口 | recognition 永远强于 recall |
| 7 | **空状态必须回答三问**：现在是什么 / 为什么为空 / 下一步做什么 | 空必须被设计，而非被忽略 |
| 8 | **视觉克制不许连内容一起减。**信息空洞在 AI 检索时代是致命项 | thin content 与 AI 引用的双重损失 |
| 9 | **动效只用于定位，不用于表演。**微交互的信息是「你在正轨上」 | 校准过的微交互，不是「极简」 |
| 10 | **交付演示行为，不演示外观。**多场景、多状态、失败态一起给 | 系统级交付被讲成形状故事的教训 |

### 0.2 五个一票否决（任一不通过即驳回）

1. 主导航藏在汉堡菜单 / 抽屉里；
2. 正文与背景对比度 < 4.5:1；
3. 空状态没有下一步动作；
4. 为「干净」删掉了关键路径入口（搜索、加购、结算、导出、客服）；
5. 暗色模式是浅色模式的颜色反转（未重新配色与验算）。

### 0.3 五条禁令

- 禁止用大面积纯白 `#FFFFFF` + 低对比灰字堆「高级感」；
- 禁止用胶囊大圆角 + 彩色阴影 + 渐变营造「精致」；
- 禁止用弹窗 / 对话框做内容展示与营销触达；
- 禁止用排行榜、消费等级、公开计数、连续打卡制造焦虑；
- 禁止把留白的「昂贵感」当作信息层级设计完成的证据（那是感知效应，不是可用性效应）。

---

## 一、设计立场：把界面当容器

### 1.1 三条核心命题

1. **设计是提问，不是灌输。**品牌界面不应在每个位置「说服用户喜欢我」，而应建立可信、可理解的结构，让用户完成自己的判断。问句、空状态、推荐排序都服务于判断，而非制造营销压力。
2. **形式从对象与语境中长出来，不是从风格清单里选出来。**一个用大面积红色、信息密度也不低的街区导览 App，依然可以是这套方法的最佳样本——因为它克制、结构驱动、本地语境优先。**照抄白色主题得不到方法。**
3. **界面是多通道信息结构，不是屏幕上的图像。**同时设计：信息层级、路径、感官线索（动效 / 语气）、以及用户需要记住多少。只优化视觉层的改动，只做了信息建筑的一层。

### 1.2 与「商业极简」的分野

| 维度 | 商业极简（不采用） | 本规范（采用） |
|---|---|---|
| 目标 | 最短距离：删到看起来干净 | 可被填充：删掉噪声，留下潜能 |
| 主体 | 设计师完成意义收敛 | 用户完成意义填充 |
| 隐喻 | 精密的工具 | 空碗 / 容器 |
| 情绪 | 冷、中性、未来感 | 带温度、有物质性、可停留 |
| 失败形态 | 复杂、混乱 | **贫乏、空洞、让人猜谜** |

### 1.3 三条常见误读（禁止）

- **把白做成色号**：`#FFFFFF` + 灰字 + 无衬线 ≠ 白。白在界面上是间距、层级与注意力分配。
- **把空做成删减**：删了元素却没留下使用潜能，产出的是贫乏页面，不是容器。
- **把风格做成依据**：某些品牌的留白是**品牌策略**，不是某一地区 Web 的普遍规范——当地主流站点信息密度反而常高于西方。不要用「安静、克制、高级」当设计依据，要说出功能依据。

---

## 二、四道门：克制必须与它们同时成立

> 任何一条不达标，「空」就从**可能性**退化为**困惑**。这四道门是 §3 之后所有规则的来源。

| 门 | 为什么必须 | 硬数据 / 门槛 |
|---|---|---|
| **清晰性** | 用户不需要猜；被隐藏的标签会让用户去记忆你的 UI | recognition > recall；标签常驻 |
| **可发现性** | 藏在一次点击之后的内容使用频率更低 | 汉堡菜单：可发现性 −50%；导航问题占移动端可用性问题 30–40%；超过 58% 的桌面站、67% 的移动站导航被评为「一般」或「差」 |
| **可访问性** | 克制品牌也必须过闸；这不是审美偏好 | WCAG AA：正文 4.5:1、大字与非文本控件 3:1；键盘全流程可达；读屏可完成主任务 |
| **内容深度** | AI 检索与生成式引用需要可抽取的结构 | 只有背景图 + 三句价值主张的页面「没有被引用的东西，等于不存在」；含结构化列表 / 引语 / 数据的页面在 AI 回答中可见度高 30–40% |

**补充约束（2026）**：quiet interface 是方向，但 **subtlety ≠ simplicity**——微交互默认不是「极简」的，是被精心校准以降低焦虑的。且过度自动化、激进个性化、「公然显示 AI 行为」会被读作侵入而非帮助。

---

## 三、Token（可直接复制）

### 3.1 间距：先设计负空间，再设计内容

基础单位 `4px`，其余为倍数，**不出现中间值**。

```css
:root{
  --s-1: 4px;   --s-2: 8px;   --s-3: 12px;  --s-4: 16px;
  --s-5: 24px;  --s-6: 32px;  --s-7: 48px;  --s-8: 64px;
  --s-9: 96px;  --s-10:128px;
}
```

| 语义 | 取值 | 说明 |
|---|---|---|
| 组内（图标↔文字） | 4–8 | |
| 控件内边距 | 8–16（大按钮可到 24） | |
| 段落间距 | 24 | ≥ 行高的 1.5 倍 |
| 组件之间 | 32 | |
| 区块之间 | 64–96 | 主区块间距不低于 48 |
| 章节之间 | 128 | 页面的「呼吸」来自这里，不来自卡片内 |
| 卡片内边距 | ≥ 卡片正文最小字号 × 2 | |
| 卡片间距 | ≥ 卡片内边距的 50% | |

> 调优启发式：大多数界面需要的留白是**加倍**，不是微调——32 试 64、16 试 32。但注意这是**感知效应**（留白越多越显贵），不能替代信息层级设计。

### 3.2 颜色：白是关系，不是色号

底用**暖近白** `#FCFCFC`，不用纯白 `#FFFFFF`（屏幕上的纯白偏冷、且与卡片无层次）。**屏幕上的「材质感」来自排版精度与留白，不来自纸张噪点纹理贴图**——纹理是印刷语言。

```css
:root{
  /* 空（基底） */
  --bg:           #FCFCFC;  /* 页面底：暖近白，非 #FFFFFF */
  --surface:      #F6F4F1;  /* 次级面：卡片/表头/代码块 */
  /* 墨（文字与图形） */
  --ink-01:       #2A2624;  /* 标题、正文强调       14.61:1 */
  --ink-02:       #303A43;  /* 正文                 11.30:1 */
  --ink-03:       #7A7369;  /* 次要文字、占位符      4.57:1 */
  /* 结构 */
  --primary:      #2F4767;  /* 主色：主按钮底、选中态、标题  9.23:1 */
  --primary-press:#243C6C;
  --border:       #E3DED8;  /* 装饰性分割线（1.30:1，仅分隔） */
  --border-strong:#8F877C;  /* 承载语义的边界：输入框、复选框  3.45:1 */
  /* 强调（唯一功能色，≤3% 面积） */
  --accent:       #FAD470;  /* 只做底/线/块，禁止作文字  1.39:1 */
  --accent-text:  #8A6510;  /* 强调色的文字替身            5.18:1 */
  --accent-soft:  #F3C7A1;  /* 暖辅：提示块底（配 --ink-02 为 7.45:1） */
  /* 语义 */
  --danger:       #A32B21;  /* 7.00:1 */
  --success:      #2E5E3A;  /* 7.37:1 */
  --warning:      #8A6510;  /* 5.18:1，同 --accent-text */
  --info:         #2F4767;  /* 9.23:1，同 --primary */
}
```

**对比度核算表**（已实算，可作为验收基线）

| 前景 / 背景 | 比值 | 允许用途 |
|---|---|---|
| `#2A2624` / `#FCFCFC` | 14.61:1 | 标题、正文 |
| `#303A43` / `#FCFCFC` | 11.30:1 | 正文 |
| `#7A7369` / `#FCFCFC` | 4.57:1 | 次要文字、占位符（下限） |
| `#2F4767` / `#FCFCFC` | 9.23:1 | 链接、标题 |
| **`#FAD470` / `#FCFCFC`** | **1.39:1** | **禁止作文字**，只能作底 / 下划线 / 色块 |
| `#2A2624` / `#FAD470` | 10.50:1 | 强调徽章（姜黄底 + 深字） |
| `#FCFCFC` / `#2F4767` | 9.23:1 | 主按钮（藏青底 + 白字） |
| `#303A43` / `#F3C7A1` | 7.45:1 | 提示块 |
| `#8F877C` / `#FCFCFC` | 3.45:1 | 输入框边界、图标（下限 3:1） |

**用色纪律**

- 面积配比：基底与留白 **≥65%**；文字与图形 **15–25%**；主色面（按钮 / 选中态 / 页头）**≤12%**；强调色 **≤3%**。
- **强调色是索引，不是装饰。**检验：单屏数一数出现次数，**≤3 处**。超过就不再是强调。
- **强调色只出现在「已经发生了值得注意的事」的位置**——状态变化、人际互动、需要索引的标记。不用于装饰性强调、不用于段落文字、不用于「让页面活泼一点」。
- 同一含义全站一个颜色，不得一处红一处橙。
- 渐变只允许用于「表达一个真实的方向或层级」；装饰性渐变禁止。

### 3.3 字体层级：只准用字号、字重、留白

字号阶梯固定 8 级，**禁止自定义中间值**。层级差异靠字号差与留白，不靠颜色。

| 级别 | 字号 / 行高 | 字重 | 用途 |
|---|---|---|---|
| display | 40 / 48 | 600 | 首页主标题（一页至多 1 个） |
| h1 | 32 / 40 | 600 | 页面标题 |
| h2 | 24 / 32 | 600 | 区块标题 |
| h3 | 20 / 30 | 600 | 卡片 / 小节标题 |
| body-lg | 18 / 30 | 400 | 长文正文 |
| body | 16 / 26 | 400 | 界面正文（默认） |
| body-sm | 14 / 22 | 400 | 次要信息、表格 |
| caption | 12 / 18 | 400/500 | 标签、脚注、元信息 |

- **字重只用 3 级**：400 / 500 / 600。不用 300（中文细体在屏上发虚），不用 700+。
- **字距**：中文不调字距；拉丁 20px 以上标题可 `-0.02em`。**不用字距营造「设计感」**。
- **字体族**（中文优先、系统优先，避免字体包体积）：@font-face 由 Astro Fonts API 在
  `astro.config.mjs` 的 `fonts` 里声明，经 `BaseLayout.astro` 的 `<Font />` 注入 `--font-wenkai` /
  `--font-wenkai-mono`；`global.css` 只保留回退栈。
  ```css
  --font-sans: var(--font-wenkai), "KaiTi", "STKaiti", -apple-system, "PingFang SC",
               "Hiragino Sans GB", "Source Han Sans SC", "Noto Sans SC", "Microsoft YaHei", sans-serif;
  --font-serif: var(--font-wenkai), "KaiTi", "STKaiti", "Songti SC", "Source Han Serif SC",
                "Noto Serif SC", serif;
  --font-mono: var(--font-wenkai-mono), ui-monospace, "SF Mono", Menlo, Consolas, monospace;
  ```
- **数字用等宽**（`font-variant-numeric: tabular-nums`）——表格与数据不跳动。

### 3.4 版面度量

| 项 | 门槛 | 说明 |
|---|---|---|
| 正文行长 | 拉丁 45–75 字符；**中文 24–36 字/行** | 中文全角宽约为拉丁 2 倍，故区间折半 |
| 容器宽度 | `max-inline-size: 66ch`（拉丁）/ `34em`（中文长文） | |
| 正文行高 | ≥ 1.6 | body 16/26 已满足 |
| 段间距 | ≥ 行高 × 1.5 | 用留白分隔段落，不用首行缩进 |
| 对齐 | 左对齐为主；居中仅用于短标题（≤30 字） | 不混用对齐制造「排版感」 |
| 长文 | 提供「预计阅读时长 / 难度」类**决策辅助**，不做推荐算法 | 只给判断材料，不催促、不推荐 |

### 3.5 圆角 / 描边 / 阴影

- **直角优先**：`--radius-0: 0` 为默认；输入框与按钮 `4px`；卡片与对话框上限 `8px`；标签可用全圆。
- **禁止大圆角胶囊按钮**（那是消费类视觉语言，与本规范基调冲突）。
- **分层优先用「边」而不是「影」**：1px 边框 > 阴影 > 两者叠加（禁止叠加）。
  ```css
  --shadow-1: 0 1px 2px  rgba(42,38,36,.06);
  --shadow-2: 0 4px 16px rgba(42,38,36,.08);
  ```
- 禁止彩色阴影、发光、拟物高光。
- **承载语义的边界必须 ≥3:1**（输入框、复选框、开关轨道、图标按钮）；纯装饰分割线可以更低。

### 3.6 动效

```css
--dur-1:100ms; --dur-2:160ms; --dur-3:240ms; --dur-4:320ms;
--ease-standard: cubic-bezier(.2,0,0,1);
--ease-in:       cubic-bezier(0,0,.2,1);
--ease-out:      cubic-bezier(.4,0,1,1);
```

- **只为一个目的**：说明「我从哪来、到哪去」，或确认状态已变化。位移上限 4–8px，不做缩放弹跳，不做视差。
- 入场 ≤320ms，退出比入场快一档。超过 400ms 的入场一律删除。
- **必须**支持 `prefers-reduced-motion: reduce`（降为 0ms 或纯 opacity，不得丢信息）。
- **禁止**：自动轮播、自动播放、装饰性粒子、加载动画占满整页。
- 微交互文案的信息是「**你在正轨上**」，不是「看多有趣」——进度提示是安抚，不是庆祝。

### 3.7 暗色模式：重新配，不是反转

```css
@media (prefers-color-scheme: dark){
  :root{
    --bg:           #1C1B1A;  /* 不用 #000：纯黑与文字对比过强、且边缘发晕 */
    --surface:      #232221;  /* 主文字 13.72:1 的对比建立在这个面上 */
    --ink-01:       #E9E5E0;  /* 13.72:1 */
    --ink-02:       #C9C4BD;
    --ink-03:       #9AA7B5;  /*  7.02:1 */
    --border:       #3A3835;  /*  1.47:1（装饰） */
    --border-strong:#6E6A64;
    --accent:       #D9B45A;  /*  8.70:1（暗底下可作文字，浅底下不可） */
    --accent-text:  #D9B45A;
    --danger:       #E08A80;  --success:#8FC79E;  --warning:#D9B45A;  --info:#A8C0DC;
  }
}
```

- 暗色不是浅色的映射：**每个色对都要重新验算**。浅底不可作文字的强调色，在暗底可能可以。
- 暗模式下降低视觉重量：少用边框、多用面差；图片必要时压亮度（不加白色蒙层）。
- 两套配色都由同一组语义变量驱动，组件内**禁止硬编码色值**。

### 3.8 中文 / 日文的性能约束

CJK 字体包含大量汉字，单个字体文件可达数 MB 到数十 MB；再叠加动画库，首屏渲染会急剧恶化。

- 自托管字体必须**子集化 + `font-display: swap`**；正文首选字重经 Astro Fonts API 生成 `preload`（其余按需下载）；
- **视觉克制本身就是性能方案——留白不需要下载**；
- 目标：首屏 JS ≤150KB(gzip)、LCP ≤2.5s（4G 移动）、无 FOIT；
- 内容站应做到**第三方追踪脚本 = 0**（同时改进速度、隐私与阅读沉浸感）。

---

## 四、结构层：把界面做成空容器

1. **首屏只承载当下最重要的决定。**一屏一件事，其余让给白。
2. **渐进披露的两条硬约束**：入口必须**可见、可预期**；展开后必须能**原路收回**。禁止把功能藏进手势、长按、悬停或搜索框。
3. **命名不预设用途。**用「橡木桌」而不是「咖啡桌」——把用途的决定权留给用户。导航与分类用对象与情境命名，不用营销话术命名。
4. **多路径而非单解释。**允许用户重排、收藏、组合、个性化，而不是只接受系统推荐。但必须同时保留一条**默认路径**，让不想配置的人也能直接完成。
5. **空状态必须回答三问**：现在是什么 / 为什么为空 / 下一步做什么。至少给出一个可点动作与一个理解现状的线索。
6. **首屏可见性优先级**：先回答「这是谁 / 这是干什么的 / 现在能做什么」，再谈氛围。
7. **以生活场景而非品类做一级导航**——不预设用途，给出可被用户认领的框架。
8. **结构一致优于局部惊艳。**同一个动作在任何页面用同一种交互；转场不制造意外。

---

## 五、组件级规范

> 通用原则：**能删的都删，删完必须补回可发现性。**所有「删除之后没有补回」的代价都是可用性事故（见 §10.4）。

### 5.1 导航

- 主导航项 **≤5**（+1 个「更多」）；移动端优先**底部标签栏**而非汉堡菜单。
- **文字标签常驻**，不用纯图标；当前页必须有**非颜色**的选中指示（字重 + 指示线）。
- **导航永远可见**，不因页面位置而消失。
- 三级以上用面包屑或返回路径表达，不用「返回上一页」兜底。

### 5.2 按钮

- 一个视图内**主按钮 = 1 个**（藏青底白字，9.23:1）；次按钮 1px 边框 + 文字色；第三级纯文字。
- 加载态保持宽度不变（防跳动）；禁用态必须仍可读，并说明为何禁用。
- 禁止：渐变按钮、彩色阴影、hover 放大、图标按钮无标签。

### 5.3 表单

- **标签常驻可见**，禁止用 placeholder 当标签。
- 校验 **inline、出现在用户正在看的位置、不打断**；错误必须答出「哪里错 / 为什么 / 怎么改」。
- 必填 / 选填显式标注；默认值可预填但不得夺走控制权。
- 提交失败**不得清空已输入内容**。

### 5.4 列表与卡片

- 分组优先用留白，其次用 1px 边框；**边框与阴影不同时用**。
- 卡片内边距 ≥ 正文最小字号 × 2；卡片间距 ≥ 内边距的 50%。
- 索引型列表优先于营销型卡片流；列表项必须能扫读（标题 + 一条关键元信息）。

### 5.5 空状态（本规范的核心组件）

必须同时具备：**现状**（这里是什么）、**原因**（为什么是空的）、**动作**（下一步）。可选：教育性内容、无数据时的替代视图。

- 允许：说明性文案 + 一个明确按钮 + 一个次级链接。
- 禁止：一张插画 + 一句俏皮话；禁止把「没有数据」写成无信息的语气词。

### 5.6 错误 / 404 / 500

同三问 + 返回路径。错误信息不得暴露堆栈或内部代号；不得只给一个「出错了」。

### 5.7 加载

- 超过 400ms 才显示加载（否则闪烁）；骨架屏形状必须匹配最终内容。
- 禁止整页旋转圈、禁止无进度提示的阻塞、禁止加载动画超过内容本身的价值。

### 5.8 表格

- 表头 sticky；数字右对齐 + 等宽数字；行高 ≥40px。
- 斑马纹与网格线**二选一**；中文单元格超出用省略号 + 可查看全文。
- 空表、加载中、部分失败三种状态都要单独设计。

### 5.9 对话框 / 提示条

- 对话框只用于「需要用户做出不可忽略的决定」；必须 Esc 可关、焦点锁定、主次动作明确。
- 提示条 3–5s 自动消失、可手动关闭、不遮挡主操作；同一时刻最多 1 条。
- 禁止：用对话框做内容展示、营销触达、进入即弹出的订阅框。

---

## 六、内容层：难度设计（ex-formation）的边界与内容下限

### 6.1 按风险等级分流

| 场景 | 策略 |
|---|---|
| **可用「制造有方向的陌生感」**（对比、时间线、可视化、提问式入口） | 文化内容、教育、品牌故事、复杂概念解读、探索型推荐、onboarding 的**非关键**环节 |
| **禁止**（必须给确定答案与显式路径） | 支付、账号恢复、医疗建议、紧急告警、权限授权、退款、删除确认 |

推荐流程：① 明确任务与风险等级 → ② 识别用户的错误心智模型 → ③ 用对比 / 情境 / 提问制造「有方向的陌生感」→ ④ 给出明确的继续路径 → ⑤ 用可用性测试区分「激发兴趣」与「造成迷失」。

### 6.2 内容深度下限

视觉克制**不许连内容一起减**。以下是最低要求：

- 每个页面至少给出**一个可被引用的具体事实**（数字、时间、明确结论、可验证的说明）；
- 不用「三词主标题 + 两段价值主张」的模板；不用薄内容凑版式；
- 结构化：列表、小标题、表格、数据点——既服务扫读，也服务 AI 检索与引用；
- 长内容提供摘要与「读多久」的决策辅助，而不是一次性倾倒。

### 6.3 署名与出处

- 内容页必须诚实可见**作者与更新日期**——署名即契约。
- 数据与结论标注来源与时间；不可靠来源明确标注，不作为事实引用。

### 6.4 本地化不是翻译

本地化要重新理解使用情境（行为习惯、计价方式、语言断行、单位与日期格式），而不是把同一套模板换语言。**词汇、图标与颜色都要重新检查跨文化歧义。**

---

## 七、交互层：让用户参与，而不是被动消费

1. **可逆性优先**：能撤销就不要确认，能预览就不要直接生效，能回退就不要阻断。破坏性操作给「撤销」而不是十次二次确认。
2. **反馈是安抚，不是庆祝**：确认用户在正轨上，不把完成变成表演。不用撒花、不用音效、不用「太棒了！」。
3. **不制造焦虑的机制**：不做消费等级、排行榜、公开计数、连续打卡、倒计时催单。把品牌价值观编码进可执行动作（回收、自带购物袋、读完一篇文章）而非消费额。
4. **规则必须一句话说得清**。把「2 万积分换 200 点」这类换算丢给用户，是把系统复杂度转嫁给用户。任何兑换率、权益规则都要做到「店员能一句话解释」。
5. **统一身份**：同一产品内的账号 / 会员 / 订单体系不得割裂（App 与 Web 各自一套 ID 是典型事故）。
6. **系统状态永远可见**：保存中 / 已保存 / 离线 / 同步失败必须有明确指示，空白的沉默不是「优雅」。

---

## 八、AI 界面层：可解释 / 可重写 / 可塑形

把「空」重新定义：不再是「没有内容」，而是「**未被系统替你决定的部分**」。

1. **可解释**：说明依据与来源，让用户能判断而不是只能接受。不假装系统全知。
2. **可重写**：允许用户改目标、改条件、要求重来；目标一旦确定就锁死的界面是黑箱。
3. **可塑形**：结果是**可继续修改的草稿**，不是不可修改的定稿。分层摘要代替一次性倾倒全部答案。
4. **保留思考的空间**：给出对比与反例、保留比较与确认的余地，而不是替用户下结论。
5. **不过度自动化**：激进个性化、抢答式预测、公然展示「AI 正在思考你」都会被读作侵入而非帮助。默认不介入，需要时再介入。
6. **AI 生成的视觉不得取代信息**：生成图只能承担氛围与解释，不能替代可被引用的文字与数据。

---

## 九、情境层：从页面到完整情境

研究对象不是「某个页面好不好看」，而是**完整场景**。设计时必须覆盖三个时刻：

| 时刻 | 必须回答 |
|---|---|
| **出发前** | 用户从哪来、预期是什么、第一次看到这个页面要立即明白什么 |
| **在现场** | 当前设备、网络条件（慢网 / 离线）、注意力是否被打断、单手是否可用 |
| **离开后** | 结果如何带走（导出 / 分享 / 凭证 / 邮件）、如何回来、如何延续 |

- **跨设备连续性**：同一任务在手机开始、桌面继续，状态不得丢失。
- **一套数据、多端多物**：同一套信息结构流向 Web、App、印刷、线下。
- **降级路径必须设计**：离线、弱网、旧浏览器、无 JS 下仍能完成核心任务。
- **把线下触点纳入**：门店、包装、客服话术与界面用同一套信息结构与措辞。
- **可携带性**：容器不该锁死内容——导出路径（Markdown / CSV / 标准格式）必须完整可查。

---

## 十、交付与评审

### 10.1 演示行为，不演示外观

当交付物是**系统与规则**时，用静态对比图汇报必然失败。交付必须包含：

- 多场景行为（同一组件在列表 / 详情 / 空态 / 错误态下的表现）；
- 多状态（默认 / 悬停 / 焦点 / 激活 / 禁用 / 加载 / 失败）；
- 动效参数（时长、缓动、位移）；
- 失败路径与降级方案。

**只给一张截图，必然被问「改了哪里」。**

### 10.2 评审门禁（硬指标，全部需实测）

| # | 指标 | 门槛 | 验证方式 |
|---|---|---|---|
| 1 | 正文对比度 | ≥4.5:1 | 按 §3.2 计算值核对 |
| 2 | 非文本控件 / 边界对比度 | ≥3:1 | 计算值 |
| 3 | 键盘全流程 | 主任务可达、焦点态可见、无焦点陷阱、跳过导航可用 | 手动走查 |
| 4 | 读屏 | 主任务可独立完成 | VoiceOver / NVDA |
| 5 | 主导航 | ≤5 项且常驻可见 | 目视 |
| 6 | 正文行长 | 中文 24–36 字/行 | 测量 |
| 7 | 单屏主按钮 | =1 | 目视 |
| 8 | 强调色 | 面积 ≤3%、单屏 ≤3 处 | 目视 |
| 9 | 暗色模式 | 独立配色 + 全部色对重算 | 计算值 |
| 10 | 空状态 / 错误态 | 三问齐备，有下一步 | 清单 |
| 11 | LCP（4G 移动） | ≤2.5s | Lighthouse |
| 12 | 首屏 JS | ≤150KB gzip | 构建产物 |
| 13 | CJK 字体 | 子集化或系统栈，无 FOIT | Network + 目视 |
| 14 | 内容站第三方脚本 | =0 | Network |
| 15 | 组件内硬编码色值 | =0（只允许变量） | 代码扫描 |

### 10.3 六个判断问题（每个界面都问一遍）

1. 它减少的是噪声，还是也误删了必要信息？
2. 用户是否拥有形成自己理解和选择的空间？
3. 设计是否从页面扩展到了完整使用情境？
4. 「陌生感」是否服务于洞察，而不是制造迷路？
5. 品牌是否通过长期一致的体验建立信任，而非不断索取注意力？
6. 留白、克制与开放性是否**同时**通过可用性、可访问性与错误恢复验证？

### 10.4 反面清单：这些代价是可验证的

| 做法 | 代价 | 实证来源 |
|---|---|---|
| 首页看不出在卖什么 | 品牌身份不清，用户「不知道这家店在卖什么」 | 对某零售品牌美国官网的界面评析 |
| 导航在某类页面消失、创建按钮无标签 | 一致性 / 可见性 / 可供性同时失败 | 对某内容收藏平台的桌面端界面评论 |
| 汉堡菜单承载主导航 | 可发现性 −50% | Nielsen Norman Group 研究 |
| 隐藏手势承担功能入口 | 低频用户永远找不到 | 某阅读平台的文章副标题入口 |
| 「更极简」但牺牲空间利用 | 社区反弹后回滚 | 某设计工具的 UI3 改版 |
| App 体验与品牌美学有落差 | 评分与口碑受损 | 某会员 App 的使用评价 |
| 极简到无法满足真实表达需求 | 用户流失（「极简对我来说太极端了」） | 某极简博客平台的用户反馈 |
| 只有背景图 + 三句价值主张 | 搜索结果与 AI 引用双重不可见 | thin content 与 AI 检索数据 |

> **共性**：所有代价都发生在「**删除之后没有补回**」的时刻——删了导航没补可发现性，删了标签没补语义，删了装饰没补性能与排版的投入，删了文案没补内容深度。

---

## 附录 A · Token 速查

```
间距  4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128
字号  12 / 14 / 16 / 18 / 20 / 24 / 32 / 40
字重  400 / 500 / 600
行高  1.5（界面）/ 1.6+（长文）/ 1.2（大标题）
圆角  0（默认）/ 4（控件）/ 8（卡片·对话框上限）
时长  100 / 160 / 240 / 320 ms
对比  正文 4.5:1 · 大字与非文本 3:1
占比  基底 ≥65% · 文字 15–25% · 主色 ≤12% · 强调 ≤3%
```

## 附录 B · 上线前清单（可打印）

```
[ ] 一屏只讲一件事；主按钮 = 1 个
[ ] 主导航 ≤5 项，文字标签常驻，导航在每一页都可见
[ ] 没有把功能藏进汉堡菜单 / 手势 / 悬停 / 只有图标无标签
[ ] 正文对比度 ≥4.5:1；控件边界 ≥3:1（已实测，不是估计）
[ ] 键盘能走完主任务；焦点态可见；有跳过导航
[ ] 读屏能走完主任务
[ ] 空状态 / 错误态回答了「现状 / 原因 / 下一步」
[ ] 表单标签常驻；错误 inline 且不打断；失败不清空输入
[ ] 暗色模式是重新配的，全部色对重算过
[ ] 强调色单屏 ≤3 处、面积 ≤3%，且未用作正文文字
[ ] 动效只用于定位与状态确认；prefers-reduced-motion 已支持
[ ] 慢网 / 离线 / 无 JS 下的降级路径已设计
[ ] 每个页面至少一个可被引用的具体事实（内容深度下限）
[ ] 导出与可携带路径完整可查
[ ] 演示材料包含多状态与失败路径，而不是一张截图
```

---

## 附：外部依据

本规范的规则来自公开的设计文本、研究与真实产品案例，主要如下（按主题归类，均为公开来源）：

**理念原始文本**

- 原研哉《白》(White, Lars Müller, 2010)、《设计中的设计》—— 白 / 空 / 信息构筑的原始表述
- Interaction 18 主题演讲《Make things unknown》：https://interaction18.ixda.org/program/keynote--kenya-hara
- 無印良品 生活良品研究所，原研哉演讲採録：https://www.muji.net/lab/report/100203-01.html
- Quartz，*Emptiness—not minimalism—is the path to creativity*：https://qz.com/quartzy/1140182/mujis-design-philosophy-is-emptiness-not-minimalism-says-kenya-hara
- designboom 访谈 *interview with designer kenya hara*：https://www.designboom.com/design/interview-with-designer-kenya-hara-07-04-2014/

**极简的可用性代价**

- DesignRush，*Minimalist Web Design Has Created a Discoverability Problem*（NN/g 汉堡菜单数据、Baymard 导航基准、thin content 与 AI 检索数据）：https://news.designrush.com/minimalist-web-design-discoverability-problem
- Bits Kingdom，*Minimalist Design: When the Interface Doesn't Show You Everything*（Figma UI3 回滚、Medium 副标题入口）：https://bitskingdom.com/blog/minimalist-ui-design-hidden-controls
- UX Horizon，*Why Minimalism Fails if You Don't Understand User Context*：https://uxhorizon.com/why-minimalism-fails-if-you-dont-understand-user-context/
- Kara Digital，*When Minimalism in App Design Goes Too Far*：https://karadigital.co/blog?p=4907
- Website of Nothing，*Nothing in Technology: The Digital Void*（空状态设计纪律）：https://websiteofnothing.com/nothing-in-technology.html

**真实产品案例**

- Pratt Institute，*Design Critique: MUJI (US Website)*：https://ixd.prattsi.org/2020/11/design-critique-muji-us-website
- Pratt Institute，*Design Critique: Are.na (Desktop Web)*：https://www.ixd.prattsi.org/2024/09/design-critique-are-na-desktop-web
- note 官方，*Why note can remain simple*：https://note.com/623px/n/nb468ad085509
- note，*Deep Dive Observation Sketch #010: note's Web Design*：https://note.com/offreco/n/n5e18fbad8935
- Standard Ebooks 官方：https://standardebooks.org/
- Bear Blog 官方：https://bearblog.dev
- 良品計画 新闻稿，MUJI passport 全面改版：https://www.ryohin-keikaku.jp/news/2025_0528_02.html
- 良品計画，MUJI GOOD PROGRAM：https://www.ryohin-keikaku.jp/corporate/articles/esg_mujigoodprogram_202509
- yapp.li 访谈（会员体系改版的三项课题、废除消费等级）：https://yapp.li/magazine/8099
- Xiaomi 官方新闻稿，*Xiaomi Unveils New "Alive" Branding Identity*：https://www.mi.com/global/discover/article?id=1660

**趋势与留白研究**

- Netwise，*Japanese Web Design Trends 2026: New Values, New Standards*：https://www.netwise.jp/?p=8493/
- Netwise，*Japanese Web Design Trends 2026: The Quiet Intelligence of Subtle UX*：https://www.netwise.jp/?p=8889/
- Technivorz，*Japanese Web Design Trends in 2026*（当地站点信息密度高于西方）：https://technivorz.com/japanese-web-design-trends-in-2026-tradition-trust-and-technological-refinement/
- Vox Animus，*Whitespace Is Confidence. Use More.*（KAIST 留白与价格感知研究的转引）：https://www.vox-animus.com/blog/whitespace-is-confidence

**标准**

- WCAG 2.1（W3C）—— 对比度 4.5:1 / 3:1、键盘可达、reduced motion 等门槛的出处

> §3.2 全部对比度比值、§3.7 暗色模式色值、灰阶与语义色取值，由本文件按 WCAG 2.1 相对亮度公式实算得出，验收时按同口径复核。

---

> **免责声明**：本文件是把设计方法论转译为工程规范的方法产出，不代表任何设计师、品牌或机构的官方设计规范。文中引用的品牌与产品仅用于说明方法论，不构成对其的官方归因或评价；涉及具体产品设计方的表述，均应区分「品牌理念持有者」与「具体产品的设计 / 执行方」。
